// type ValidationErrorType = Error & { errors: string[] };

// export default async function handle_response(response) {
//   const data = await response.json();
//   if (data.errors) {
//     const error_arr = data.errors.map((error) => error.msg);
//   }
// }
